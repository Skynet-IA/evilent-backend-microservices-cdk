// 🧪 Tests para la clase Validation
//
// Este es tu PRIMER TEST del proyecto. Aquí aprenderás:
// • Cómo estructurar un test (Arrange-Act-Assert)
// • Cómo usar expect() para verificar resultados
// • Cómo organizar tests con group()

import 'package:flutter_test/flutter_test.dart';
import 'package:evilent_app/core/utils/validation.dart';

void main() {
  // ┌─────────────────────────────────────────────────────────────────────────┐
  // │ 📧 TESTS DE VALIDACIÓN DE EMAIL                                          │
  // └─────────────────────────────────────────────────────────────────────────┘
  
  group('Validation - validateEmail', () {
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST 1: Email válido debe pasar ✅
    // ────────────────────────────────────────────────────────────────────────
    test('Email válido debe retornar null', () {
      // 🎯 ARRANGE (Preparar)
      // Definimos el dato de entrada (un email válido)
      final email = 'usuario@ejemplo.com';
      
      // 🎯 ACT (Actuar)
      // Ejecutamos la función que queremos testear
      final resultado = Validation.validateEmail(email);
      
      // 🎯 ASSERT (Verificar)
      // Verificamos que el resultado es el esperado
      // null = válido (sin errores)
      expect(resultado, null);
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST 2: Email vacío debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Email vacío debe retornar error', () {
      // 🎯 ARRANGE
      final email = '';
      
      // 🎯 ACT
      final resultado = Validation.validateEmail(email);
      
      // 🎯 ASSERT
      // Esperamos que retorne un mensaje de error
      expect(resultado, 'El email es requerido');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST 3: Email sin formato correcto debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Email sin @ debe retornar error', () {
      // 🎯 ARRANGE
      final email = 'usuarioejemplo.com'; // Falta el @
      
      // 🎯 ACT
      final resultado = Validation.validateEmail(email);
      
      // 🎯 ASSERT
      expect(resultado, 'Ingresa un email válido');
    });
    
    test('Email null debe retornar error', () {
      // 🎯 ARRANGE
      final String? email = null;
      
      // 🎯 ACT
      final resultado = Validation.validateEmail(email);
      
      // 🎯 ASSERT
      expect(resultado, 'El email es requerido');
    });
  });

  // ┌─────────────────────────────────────────────────────────────────────────┐
  // │ 🔐 TESTS DE VALIDACIÓN DE CONTRASEÑA                                     │
  // └─────────────────────────────────────────────────────────────────────────┘
  
  group('Validation - validatePassword', () {
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Contraseña válida debe pasar ✅
    // ────────────────────────────────────────────────────────────────────────
    test('Contraseña válida debe retornar null', () {
      // 🎯 ARRANGE
      // Contraseña que cumple todos los requisitos:
      // • 8+ caracteres
      // • Mayúscula (A)
      // • Minúscula (bc)
      // • Número (123)
      // • Carácter especial (!)
      final password = 'Abc123!@';
      
      // 🎯 ACT
      final resultado = Validation.validatePassword(password);
      
      // 🎯 ASSERT
      expect(resultado, null);
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Contraseña vacía debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Contraseña vacía debe retornar error', () {
      // 🎯 ARRANGE
      final password = '';
      
      // 🎯 ACT
      final resultado = Validation.validatePassword(password);
      
      // 🎯 ASSERT
      expect(resultado, 'La contraseña es requerida');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Contraseña corta debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Contraseña con menos de 8 caracteres debe retornar error', () {
      // 🎯 ARRANGE
      final password = 'Abc1!'; // Solo 5 caracteres
      
      // 🎯 ACT
      final resultado = Validation.validatePassword(password);
      
      // 🎯 ASSERT
      expect(resultado, 'Mínimo 8 caracteres');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Contraseña sin mayúscula debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Contraseña sin mayúscula debe retornar error', () {
      // 🎯 ARRANGE
      final password = 'abc123!@'; // Sin mayúscula
      
      // 🎯 ACT
      final resultado = Validation.validatePassword(password);
      
      // 🎯 ASSERT
      expect(resultado, 'Debe contener una mayúscula');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Contraseña sin minúscula debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Contraseña sin minúscula debe retornar error', () {
      // 🎯 ARRANGE
      final password = 'ABC123!@'; // Sin minúscula
      
      // 🎯 ACT
      final resultado = Validation.validatePassword(password);
      
      // 🎯 ASSERT
      expect(resultado, 'Debe contener una minúscula');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Contraseña sin número debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Contraseña sin número debe retornar error', () {
      // 🎯 ARRANGE
      final password = 'Abcdefg!'; // Sin número
      
      // 🎯 ACT
      final resultado = Validation.validatePassword(password);
      
      // 🎯 ASSERT
      expect(resultado, 'Debe contener un número');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Contraseña sin carácter especial debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Contraseña sin carácter especial debe retornar error', () {
      // 🎯 ARRANGE
      final password = 'Abc12345'; // Sin carácter especial
      
      // 🎯 ACT
      final resultado = Validation.validatePassword(password);
      
      // 🎯 ASSERT
      expect(resultado, 'Debe contener un carácter especial');
    });
  });

  // ┌─────────────────────────────────────────────────────────────────────────┐
  // │ 📝 TESTS DE VALIDACIÓN DE CAMPOS REQUERIDOS                              │
  // └─────────────────────────────────────────────────────────────────────────┘
  
  group('Validation - validateRequired', () {
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Campo con contenido debe pasar ✅
    // ────────────────────────────────────────────────────────────────────────
    test('Campo con texto debe retornar null', () {
      // 🎯 ARRANGE
      final value = 'Algún texto';
      
      // 🎯 ACT
      final resultado = Validation.validateRequired(value);
      
      // 🎯 ASSERT
      expect(resultado, null);
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Campo vacío debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Campo vacío debe retornar error', () {
      // 🎯 ARRANGE
      final value = '';
      
      // 🎯 ACT
      final resultado = Validation.validateRequired(value);
      
      // 🎯 ASSERT
      expect(resultado, 'Este campo es requerido');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Campo con espacios vacíos debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Campo con solo espacios debe retornar error', () {
      // 🎯 ARRANGE
      final value = '   '; // Solo espacios
      
      // 🎯 ACT
      final resultado = Validation.validateRequired(value);
      
      // 🎯 ASSERT
      expect(resultado, 'Este campo es requerido');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Campo null debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Campo null debe retornar error', () {
      // 🎯 ARRANGE
      final String? value = null;
      
      // 🎯 ACT
      final resultado = Validation.validateRequired(value);
      
      // 🎯 ASSERT
      expect(resultado, 'Este campo es requerido');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Campo con nombre personalizado debe mostrar ese nombre ✅
    // ────────────────────────────────────────────────────────────────────────
    test('Campo vacío con nombre personalizado debe mostrar nombre en error', () {
      // 🎯 ARRANGE
      final value = '';
      final fieldName = 'Nombre completo';
      
      // 🎯 ACT
      final resultado = Validation.validateRequired(value, fieldName: fieldName);
      
      // 🎯 ASSERT
      expect(resultado, 'Nombre completo es requerido');
    });
  });

  // ┌─────────────────────────────────────────────────────────────────────────┐
  // │ 🔢 TESTS DE VALIDACIÓN DE CÓDIGO DE VERIFICACIÓN                         │
  // └─────────────────────────────────────────────────────────────────────────┘
  
  group('Validation - validateVerificationCode', () {
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Código válido de 6 dígitos debe pasar ✅
    // ────────────────────────────────────────────────────────────────────────
    test('Código de 6 dígitos debe retornar null', () {
      // 🎯 ARRANGE
      final code = '123456';
      
      // 🎯 ACT
      final resultado = Validation.validateVerificationCode(code);
      
      // 🎯 ASSERT
      expect(resultado, null);
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Código vacío debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Código vacío debe retornar error', () {
      // 🎯 ARRANGE
      final code = '';
      
      // 🎯 ACT
      final resultado = Validation.validateVerificationCode(code);
      
      // 🎯 ASSERT
      expect(resultado, 'El código es requerido');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Código con menos de 6 dígitos debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Código con menos de 6 dígitos debe retornar error', () {
      // 🎯 ARRANGE
      final code = '12345'; // Solo 5 dígitos
      
      // 🎯 ACT
      final resultado = Validation.validateVerificationCode(code);
      
      // 🎯 ASSERT
      expect(resultado, 'El código debe tener 6 dígitos');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Código con más de 6 dígitos debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Código con más de 6 dígitos debe retornar error', () {
      // 🎯 ARRANGE
      final code = '1234567'; // 7 dígitos
      
      // 🎯 ACT
      final resultado = Validation.validateVerificationCode(code);
      
      // 🎯 ASSERT
      expect(resultado, 'El código debe tener 6 dígitos');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Código con letras debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Código con letras debe retornar error', () {
      // 🎯 ARRANGE
      final code = '12A456'; // Contiene letra 'A'
      
      // 🎯 ACT
      final resultado = Validation.validateVerificationCode(code);
      
      // 🎯 ASSERT
      expect(resultado, 'El código debe contener solo números');
    });
    
    // ────────────────────────────────────────────────────────────────────────
    // TEST: Código con caracteres especiales debe fallar ❌
    // ────────────────────────────────────────────────────────────────────────
    test('Código con caracteres especiales debe retornar error', () {
      // 🎯 ARRANGE
      final code = '123!56'; // Contiene '!'
      
      // 🎯 ACT
      final resultado = Validation.validateVerificationCode(code);
      
      // 🎯 ASSERT
      expect(resultado, 'El código debe contener solo números');
    });
  });
}

