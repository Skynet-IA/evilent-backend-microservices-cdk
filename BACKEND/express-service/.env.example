# =====================================================
# EXPRESS-SERVICE - VARIABLES DE ENTORNO
# =====================================================
# 
# INSTRUCCIONES:
# 1. Copiar este archivo a .env.local
# 2. Reemplazar valores XXX con tus credenciales reales
# 3. NUNCA committear .env.local o .env (agregar a .gitignore)
# 4. Mantener credenciales en Secrets Manager para producción
#
# REGLA #2 (Backend Rules): Sin datos sensibles hardcodeados

# =====================================================
# DATABASE - PostgreSQL
# =====================================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=evilent_user
DB_PASSWORD=secure_password_change_in_prod
DB_NAME=express_service_db
DB_SSL=false

# Tests (base de datos separada)
DB_TEST_HOST=localhost
DB_TEST_PORT=5432
DB_TEST_USER=evilent_user
DB_TEST_PASSWORD=secure_password_change_in_prod
DB_TEST_NAME=express_service_db

# =====================================================
# COGNITO - AWS User Pool
# =====================================================
# Para obtener Pool ID: AWS Console > Cognito > User Pools > Pool ID (ej: us-east-1_XXXXXXXXX)
# Para obtener Client ID: App clients > Client ID
COGNITO_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_APP_CLIENT_ID=1a2b3c4d5e6f7g8h9i0j
COGNITO_REGION=us-east-1

# TESTING - Cognito Test Pool (para E2E tests con tokens reales)
COGNITO_POOL_ID_TEST=us-east-1_YYYYYYYYY
COGNITO_CLIENT_ID_TEST=2k3l4m5n6o7p8q9r0s1t

# =====================================================
# SERVER
# =====================================================
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug
SERVICE_NAME=express-service

# =====================================================
# SEGURIDAD
# =====================================================
CORS_ORIGIN=http://localhost:3001
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

